<!-- Drink About Us Section -->
<style>
.drink-about {
  padding: 120px 48px;
  background: #FFFFFF;
  position: relative;
  overflow: hidden;
  font-family: var(--base-font-family), -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  box-sizing: border-box;
}
.drink-about * { box-sizing: border-box; margin: 0; padding: 0; }
.drink-about::before {
  content: '';
  position: absolute;
  top: 0; right: 0;
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(255,229,102,0.1) 0%, transparent 70%);
  border-radius: 50%;
}
.drink-about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
  max-width: 1300px;
  margin: 0 auto;
}
.drink-about-img {
  width: 100%;
  border-radius: 16px;
  overflow: hidden;
  aspect-ratio: 4/3;
  position: relative;
}
.drink-about-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  border-radius: 16px;
}
.drink-about-eyebrow {
  font-size: 13px;
  font-weight: 700;
  letter-spacing: 2.5px;
  text-transform: uppercase;
  color: #777777;
  margin-bottom: 16px;
  text-align: left;
}
.drink-about-content h2 {
  font-family: var(--heading-font-family), serif;
  font-size: 52px;
  font-weight: 700;
  line-height: 1.1;
  margin-bottom: 24px;
  color: #1A1A1A;
}
.drink-about-content p {
  font-size: 18px;
  line-height: 1.7;
  color: #555555;
  margin-bottom: 20px;
}
.drink-about-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  margin: 36px 0;
  padding: 32px 0;
  border-top: 1px solid #EEEEEE;
  border-bottom: 1px solid #EEEEEE;
}
.drink-stat-number {
  font-family: var(--heading-font-family), serif;
  font-size: 40px;
  font-weight: 800;
  color: #FFE566;
  line-height: 1;
}
.drink-stat-label {
  font-size: 14px;
  color: #555555;
  margin-top: 4px;
  font-weight: 500;
}
.drink-about .drink-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: #1A1A1A;
  color: #FFFFFF;
  padding: 18px 36px;
  border-radius: 8px;
  font-size: 17px;
  font-weight: 700;
  text-decoration: none;
  transition: all 0.25s;
}
.drink-about .drink-btn:hover {
  background: #333333;
  transform: translateY(-2px);
}
@media (max-width: 1024px) {
  .drink-about-grid { grid-template-columns: 1fr; gap: 48px; }
  .drink-about-content h2 { font-size: 40px; }
}
@media (max-width: 768px) {
  .drink-about { padding: 64px 20px; }
  .drink-about-grid { gap: 40px; }
  .drink-about-content h2 { font-size: 32px; }
  .drink-about-content p { font-size: 16px; }
  .drink-about-stats { grid-template-columns: repeat(3, 1fr); gap: 16px; padding: 24px 0; }
  .drink-stat-number { font-size: 32px; }
  .drink-stat-label { font-size: 12px; }
}
@media (max-width: 480px) {
  .drink-about-content h2 { font-size: 28px; }
  .drink-about-stats { gap: 8px; }
  .drink-stat-number { font-size: 26px; }
  .drink-stat-label { font-size: 11px; }
}
</style>

<section class="drink-about">
  <div class="drink-about-grid">
    <div class="drink-about-img">
      {% if section.settings.image != blank %}
        <img src="{{ section.settings.image | image_url: width: 1200 }}" alt="{{ section.settings.image.alt | default: section.settings.heading }}" width="{{ section.settings.image.width }}" height="{{ section.settings.image.height }}" loading="lazy">
      {% endif %}
    </div>
    <div class="drink-about-content">
      <div class="drink-about-eyebrow">{{ section.settings.eyebrow }}</div>
      <h2>{{ section.settings.heading }}</h2>
      <p>{{ section.settings.description_1 }}</p>
      <p>{{ section.settings.description_2 }}</p>
      <div class="drink-about-stats">
        {% for block in section.blocks %}
          {% if block.type == 'stat' %}
            <div {{ block.shopify_attributes }}>
              <div class="drink-stat-number">{{ block.settings.number }}</div>
              <div class="drink-stat-label">{{ block.settings.label }}</div>
            </div>
          {% endif %}
        {% endfor %}
      </div>
      {% if section.settings.button_label != blank %}
        <a href="{{ section.settings.button_url }}" class="drink-btn">{{ section.settings.button_label }}</a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Drink About Us",
  "class": "section-drink-about-us",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Main Image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow Text",
      "default": "About DRiNK"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "We make hydration powder."
    },
    {
      "type": "textarea",
      "id": "description_1",
      "label": "Description (Paragraph 1)",
      "default": "Electrolytes. B-vitamins. Five flavors. No sugar, no caffeine, no fillers. Just mix it with water and move on."
    },
    {
      "type": "textarea",
      "id": "description_2",
      "label": "Description (Paragraph 2)",
      "default": "Every tub has 30 servings. Every serving has the same clean formula. We kept it simple because hydration should be."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Shop All Flavors"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Stat",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number",
          "default": "100+"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Stat Label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Drink About Us",
      "blocks": [
        {
          "type": "stat",
          "settings": {
            "number": "5",
            "label": "Flavors"
          }
        },
        {
          "type": "stat",
          "settings": {
            "number": "0g",
            "label": "Sugar"
          }
        },
        {
          "type": "stat",
          "settings": {
            "number": "6",
            "label": "Electrolytes"
          }
        }
      ]
    }
  ]
}
{% endschema %}
